import React from 'react';
import { CharactersList } from '../../components/CharactersList/CharactersList';

export const CharactersListScreen = ({ navigation }) => (
  <CharactersList navigation={navigation} />
);
